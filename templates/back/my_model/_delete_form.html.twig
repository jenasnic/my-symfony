<form method="post" action="{{ path('back_my_model_delete', {'id': my_model.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ my_model.id) }}">
    <button class="btn">Delete</button>
</form>
